import styled from 'styled-components';
import { flexbox, space } from 'styled-system';

export const SUIBox = styled.div`
  display: flex;
  flex-direction: row;
  gap: 1rem;
  ${flexbox}
  ${space}
`;
